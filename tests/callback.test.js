const {test} = QUnit;
QUnit.module('Callback');

/**
 * Tests related to callback creation
 */

test("callback creation", assert=>{

});

test("visual representation of a callback", assert=>{

});


/**
 * Tests related to configuring the callback
 */

test("set callback name", assert=>{

});

test("apply a design pattern to the callback", assert=>{

});

test("delete the callback", assert=>{

});

